{% extends 'base.html.twig' %}
{% block back %}
<li id="help"><a href="">How to Play</a></li>
<li class="endGame"><a href="" id="endGame">Fine Partita</a></li>
{% endblock %}
{% block body %}

<div class="row">
  <h4>Qual &egrave; la bandiera corretta?</h4>
</div>
<div class="row">
  <div class="col-xs-12 buttonContainer">
    <h3>{{country.name}}</h3>
    <span style="color:
            {% if points>0 %}  lightgreen;"
            {% elseif points<0 %} #f00;"
            {% else %} #fff;"
            {% endif %}>Punteggio: {{points}}
    </span>&nbsp;&nbsp;&nbsp;
    <span style="color:
            {% if errors > 1 %} lightgreen;"
            {% elseif errors == 1 %} #FAD813;"
            {% else %} #f00;"
            {% endif %}>Vite: {{errors}}
  </div>
  <div class="col-xs-6 buttonContainer">
    <form name="game" method="post" action=""/>
      <input type="hidden" name="answer" value="{{country.name}}" />
      <input type="hidden" name="guess" value="{{flags[0].name}}" />
      <input type="hidden" name="points" value="{{points}}" />
      <input type="hidden" name="errors" value="{{errors}}" />
      <input type="image" class="smallFlags" src="{{asset('img/flags/'~flags[0].flagsrc~'.png')}}" alt="???" title="???" />
    </form>
    <form name="game" method="post" action=""/>
      <input type="hidden" name="answer" value="{{country.name}}" />
      <input type="hidden" name="guess" value="{{flags[1].name}}" />
      <input type="hidden" name="points" value="{{points}}" />
      <input type="hidden" name="errors" value="{{errors}}" />
      <input type="image" class="smallFlags" src="{{asset('img/flags/'~flags[1].flagsrc~'.png')}}" alt="???" title="???" />
    </form>
    </div>
    <div class="col-xs-6 buttonContainer">
    <form name="game" method="post" action=""/>
      <input type="hidden" name="answer" value="{{country.name}}" />
      <input type="hidden" name="guess" value="{{flags[2].name}}" />
      <input type="hidden" name="points" value="{{points}}" />
      <input type="hidden" name="errors" value="{{errors}}" />
      <input type="image" class="smallFlags" src="{{asset('img/flags/'~flags[2].flagsrc~'.png')}}" alt="???" title="???" />
    </form>
    <form name="game" method="post" action=""/>
      <input type="hidden" name="answer" value="{{country.name}}" />
      <input type="hidden" name="guess" value="{{flags[3].name}}" />
      <input type="hidden" name="points" value="{{points}}" />
      <input type="hidden" name="errors" value="{{errors}}" />
      <input type="image" class="smallFlags" src="{{asset('img/flags/'~flags[3].flagsrc~'.png')}}" alt="???" title="???" />
    </form>
  </div>
</div>
{% endblock %}
{% block endGameLayer %}
<div id="endGameLayer" style="{{display}}">
  <div class="modale">
        <h4>GAME OVER<br />Hai totalizzato {{points}} punti!</h4>
        <form action="/records/set/{{setScelto}}" method="post" >
        <input type="text" name="playerName" placeholder="Inserisci nome" class="player" required />
        <input type="hidden" name="mode" value="ntf" />
        <input type="hidden" name="points" value="{{points}}" />
        <input type="hidden" name="errors" value="{{errors}}" />
        <input type="submit" class="choices" value="FATTO" />
        </form>
  </div>
</div>
{% endblock %}
{% block modal %}
  <div id="helpModal" class="modal">
    <p class="valign">In questa modalita' ti verra' fornito il nome di uno stato e dovrai indovinare qual e' la bandiera corretta tra le 4 proposte.
      <br /><br />Non preoccuparti: non ci sono limiti di tempo!
      <br /><br />Per ogni risposta corretta ti verra' assegnato un punto, mentre per ogni risposta sbagliata un punto verra sottratto al totale.
      Riuscirai ad entrare nella TOP 10?  Raccogli la sfida!</p>
    <h6 class="modalClose">OK!</h6>
  </div>
  <div id="endGameModal" class="modal">
        <h4>Vuoi terminare la partita con {{points}} punti?</h4>
        <form action="/records/set/{{setScelto}}" method="post" >
        <input type="text" name="playerName" placeholder="Inserisci nome" class="player" required />
        <input type="hidden" name="mode" value="ntf" />
        <input type="hidden" name="points" value="{{points}}" />
        <input type="hidden" name="errors" value="{{errors}}" />
        <input type="submit" class="choices" value="SI" />
        </form>
        <input type="submit" class="choices modalClose" value="NO" />
  </div>
{% endblock %}
